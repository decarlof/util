To convert a fast radiography hdf5 file in a stack of narmalized tiff images:

    python normalize.py -h
    usage: normalize.py [-h] fname

    positional arguments:
       fname       file name of a single dataset to normalize: /data/sample.h5

    optional arguments:
    -h, --help  show this help message and exit
